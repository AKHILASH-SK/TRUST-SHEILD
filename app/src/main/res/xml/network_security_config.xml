<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext traffic to ANY IP for local testing so it doesn't break when IP changes -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
