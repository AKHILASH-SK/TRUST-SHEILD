<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions required for TrustShield functionality -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrustShield"
        android:networkSecurityConfig="@xml/network_security_config">
        
        <!-- Main Activity - Router/Splash Screen (Launcher) -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TrustShield">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Login Activity -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.TrustShield.Login" />

        <!-- Registration Activity - User sign-up -->
        <activity
            android:name=".activities.RegistrationActivity"
            android:exported="false"
            android:label="Register"
            android:theme="@style/Theme.TrustShield" />

        <!-- Home Activity - Main dashboard after login -->
        <activity
            android:name=".activities.HomeActivity"
            android:exported="false"
            android:theme="@style/Theme.TrustShield" />

        <!-- Link Detail Activity - Show full link analysis -->
        <activity
            android:name=".activities.LinkDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.TrustShield" />

        <!-- Profile Activity - User profile and settings -->
        <activity
            android:name=".activities.ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.TrustShield" />

        <!-- Main Activity (kept for backward compatibility) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.TrustShield" />

        <!-- NotificationListener Service to extract messages from external apps -->
        <service
            android:name=".NotificationListener"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>

</manifest>